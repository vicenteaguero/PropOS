# PropOS v0.1.0 — Release Notes

Status of each capability in the v0.1.0 spec (`docs/versions/PropOS v0.1.0.pdf`,
local only) after the completion work. "Done" = end-to-end reachable by a user in
the UI today; "Partial" = some flows of the feature are missing a surface;
"Config" = shipped but requires environment/credential setup to activate.

> **Verified 2026-07-02** against the code at `201e586` (audit R2). Percentages
> are DONE + 0.5×PARTIAL over the spec bullets for that feature, excluding the
> explicitly deferred ones. An earlier revision of this file marked F2/F3/F7 as
> "Done"; that was optimistic and is corrected below — the gaps are UI surfaces,
> not backend endpoints.

## Feature matrix

| Area | Status | Notes |
|------|--------|-------|
| **Baseline — Auth** | Done | Supabase Auth: login, reset, session. |
| **Baseline — PWA** | Done | Installable; camera; **push now wired** (`push-sw.js` + `usePushSubscription`). Requires `VITE_VAPID_PUBLIC_KEY`. |
| **Baseline — Postgres** | Done | 76 migrations (numbering skips 039). |
| **Baseline — Storage** | Done | `documents` and `media` both private, four tenant-scoped policies each, reads through signed URLs. `media` was public and anonymously enumerable until the R3 remediation. |
| **Baseline — Multi-tenant** | Partial (96%) | 59 tables carry RLS, 189 policies. The backend still connects as service-role (BYPASSRLS), so app-path isolation rests on per-request `tenant_id` filters; policies are not the backstop there. Two places where RLS *is* load-bearing now work: views run with `security_invoker`, and `agent_readonly` (text-to-SQL) is NOBYPASSRLS with its own policies. |
| **F1 — Control con IA (Propo/agent)** | Done (100%) | PWA chat + WhatsApp; classify→resolve→dispatch; admin-gated at the API. 12 intents. Groq-only. Free-form analytics (`text_to_sql`) returns rows for the first time: `agent_readonly` now has least-privilege grants on the 17 exposed tables plus per-tenant policies, and the query sets `app.current_tenant_id`. Cost tracking, per-turn caching, LLM timeouts, retry/backoff and audit attribution on auto-commit all landed. |
| **F2 — CRM** | Done (95%) | Contacts, interactions, opportunities kanban, unified bandeja, pipeline + metrics tabs, email inbox **and compose** (Resend). The metrics tab is now gated by scope instead of 403-ing for AGENT. *Remaining:* arriendo/venta split in metrics. |
| **F3 — Productividad** | Done (92%) | Calendar with edit/delete/type selector, Tasks with `remind_at` and entity picker, Notes, Reminders + cron push (now actually scheduled). *Remaining:* payment due dates in the calendar feed. |
| **F4 — Documentos** | Done (92%) | Scanner, QR/magic-link share, versioning, owner view/download — the owner path now goes through `documents/access.py`, which intersects `property_grants` instead of exposing the whole tenant catalogue. |
| **F5 — Administrador de Datos** | Done (85%) | CSV import for contacts, transactions **and properties** (the historical backfill is now possible), AI `update_person`, audit trail, entity timeline linked from detail pages. *Deferred:* generic table browser, AI `update_property`/`complete_task`/`delete_record`. |
| **F6 — Plataforma Inmobiliaria** | Done (88%) | Property CRUD, AI description generator, and a real photo gallery: upload, `media_assets`→`media_files` join, lightbox, signed URLs. The WhatsApp→property photo path had **two** breaks — the missing read surface the audit found, and a CHECK constraint that had been silently rejecting every write since the feature shipped. Both fixed. *Deferred:* portal-publish APIs, photo editor. |
| **F7 — Gestión Financiera** | Done (85%) | Payment lifecycle, commission calc (IVA 19%), WON→receivable hook, finance UI with description and receipt upload, so the "Boletas" filter returns rows. Monthly aggregation groups by currency and anchors the month to America/Santiago. *Remaining:* expenses/taxes in the commission formula. |

## Required environment variables (new in v0.1.0)

| Variable | Where | Purpose |
|----------|-------|---------|
| `VITE_VAPID_PUBLIC_KEY` | frontend (Vercel, both projects) | Web-push subscription. Backend already holds the private key. |
| `INTERNAL_JOBS_SECRET` | backend (Cloud Run) | Auth for Cloud Scheduler → `/internal/jobs/*`. |
| `EMAIL_SYNC_ENABLED` | backend | Toggle IMAP email sync. |
| `EMAIL_IMAP_HOST/PORT/USER/PASSWORD` | backend (secrets) | Titan mailbox credentials. |
| `EMAIL_SYNC_TENANT_ID` | backend | Tenant that owns the synced mailbox. |

See `docs/cron-jobs.md` for the Cloud Scheduler setup (reminders + email sync).

## Cron jobs

Two Cloud Scheduler jobs (5-min cadence) hit internal endpoints:
- `run-due-reminders` — push reminders for events/tasks/payments.
- `email-sync` — IMAP poll → portal-lead threading into CRM.

**Neither job exists in the project yet** — the endpoints are live, the schedulers
were never created. A third job (`refresh-analytics`) is still missing its
endpoint, so the CRM/finance materialized views only refresh on a manual button
press. See `docs/cron-jobs.md`.

## Production status

Turned on and verified live during the R3 remediation (2026-08-18):

| Capability | State | Evidence |
|---|---|---|
| Secrets in Cloud Run | **13 mounted** (was 7) | `gcloud run services describe` counts 13 `valueFrom` |
| WhatsApp (Kapso) | credentials armed | `kapso-api-key`, `kapso-webhook-secret`, `kapso-phone-number-id` mounted |
| Outbound email (Resend) | **verified end to end** | test message accepted, id returned. Sender corrected to `no-reply@anaida.cl` — `propos.dev` was never a verified domain, so every send would have failed |
| Reminders cron | **running** | Cloud Scheduler `propos-reminders`, `*/5`, us-central1 |
| Analytics refresh | **running** | `propos-refresh-analytics`, `*/15`; endpoint returns `{"refreshed":true}` |
| Free-form analytics | **working** | `agent_readonly` returns its own tenant's rows and zero of the other's |
| Readiness probe | real | `/health/ready` checks database and secrets; `/health` stays a liveness constant |
| Max instances | 3 × 1Gi | was 1 × 512Mi for webhooks, crons and two frontends |

**Deliberately still off:**

- **Titan / IMAP email-sync.** `EMAIL_SYNC_ENABLED=false`, no IMAP credentials provisioned, no cron job created. The owner's call: the mailbox is delicate production and out of scope. The code-side bugs were fixed and tested against fixtures.
- **WhatsApp inbound.** The last switch is the webhook URL in the Kapso dashboard, which has no API. Until it is set, no inbound traffic arrives even though the credentials are mounted.

## Explicitly NOT in v0.1.0

- Portal publishing APIs (F6 is content package + manual posting).
- LLM provider abstraction — **Groq-only** (Cerebras/Anthropic swap deferred).
- Web Speech API (audio is MediaRecorder → Groq Whisper).
- Calendar recurrence; WhatsApp reminder channel (enum reserved, not delivered).
- Multi-account / per-tenant IMAP creds; IMAP APPEND to Sent; HTML email render (text-only).
- Auto-opportunity creation from portal leads (sync creates the contact and the email thread only — no interaction row either, despite what earlier notes claimed).
- Receipt OCR; bank reconciliation.
- Generic data-table browser; AI update_property / complete_task / delete_record.
- Property photo-edit pipeline (upload via WhatsApp agent works; in-app editor deferred).

## Security note

P0 closed a real hole: the conversational agent (`/v1/agent/*`) and proposal
acceptance (`/v1/pending/*`) were callable by **any** authenticated tenant user.
They are now `require_role`-gated. CRM/finance routers are role-gated too.

Still open at v0.1.0 (tracked in the private audit reports, see
`docs/audits/README.md`): CRM/finance routers check the role but not the
per-admin scope; the backend runs as service-role so RLS is not a backstop; and
owner/buyer document access is filtered in Python rather than against
`property_grants`. **Do not load real customer data until these close.**
