#!/bin/bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

bash "${SCRIPT_DIR}/scripts/log.sh" MAKE "🚀" "Starting PropOS dev environment"
bash "${SCRIPT_DIR}/scripts/check_env.sh" && docker-compose -f "${SCRIPT_DIR}/docker-compose.yml" up --build
