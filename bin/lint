#!/bin/bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

bash "${SCRIPT_DIR}/scripts/log.sh" MAKE "🔍" "Running linters"
cd "${SCRIPT_DIR}/backend" && poetry run ruff check .
cd "${SCRIPT_DIR}/frontend" && npm run lint
