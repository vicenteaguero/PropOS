#!/bin/bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

bash "${SCRIPT_DIR}/scripts/log.sh" MAKE "📝" "Seeding dev data"
supabase db reset --project-ref "${SUPABASE_PROJECT_ID}"
